[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ChatConIA"
version = "0.1"
dependencies = [
    "google-generativeai",
    "pyttsx3"
]

[tool.setuptools]
packages = ["ChatConIA"]
